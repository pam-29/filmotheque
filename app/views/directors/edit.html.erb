<% content_for :title, "Editing director" %>

<div class="container my-5">
  <div class="row justify-content-center">
        <div class="col-md-8">
            <h1 class="mb-4">Modifier le producteur</h1>
              <div class="mb-3">
                <%= render "form", director: @director %>
                </div>
            <div class="d-flex gap-2">
                <%= link_to "Voir ce producteur", @director, class: "btn btn-primary flex-fill" %>
                <%= link_to "Revenir aux producteurs", directors_path, class: "btn btn-secondary flex-fill" %>
            </div>
        </div>
    </div>
</div>
